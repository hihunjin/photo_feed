# Frontend

Minimal React scaffold for the Photo Feed app.

## Scripts
- `npm run dev` - Start the Vite dev server
- `npm run build` - Build for production
- `npm run preview` - Preview the build

## Notes
- The app proxies `/api` and `/media` to the backend during development.
- This scaffold keeps the current backend intact while moving the UI toward the planned React migration.
